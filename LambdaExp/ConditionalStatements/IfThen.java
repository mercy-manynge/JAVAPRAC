package ConditionalStatements;

public class IfThen {
    public static void main(String[] args) {
    int a = 2; 
    int b = 6;
    if (b < a)//Curly braces are not required if there is one return statement
    /*The below is the then Statement */
    System.out.println("result");//If the conditional block matches the requirement return the result
    else // If it doesn't, return the noresult statement
    System.out.println("No result");
    
    }
    
}
